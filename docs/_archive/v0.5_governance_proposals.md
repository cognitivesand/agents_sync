# v0.5 governance edit proposals — applied 2026-05-18

> **Status: all twelve proposals (P1–P12) have been APPLIED.** This document is now a historical record of what was proposed, what was decided during review, and where each edit landed. Two proposals were revised during the user review (P11 dropped its AC-5 provenance carve-out and rewrote AC-9 → AC-8 for per-slot archive; P4–P5 and the new P6–P8 were made generic across all available agentic_tools rather than per-version concrete tool enumerations) — the revisions are recorded inline in the relevant sections below.
>
> | Proposal | Status | Landed in |
> |---|---|---|
> | P1 — glossary entry for `customization_type` | APPLIED | `783b5f3` |
> | P2 — release-history entry for v0.5 | APPLIED | `783b5f3` |
> | P3 — out-of-scope bullet for Cursor SQLite surfaces | APPLIED | `783b5f3` |
> | P4 — FR-05 amendment (revised to generic shape) | APPLIED | `879815d` |
> | P5 — FR-06 amendment (revised to generic shape) | APPLIED | `879815d` |
> | P6 — FR-09 new (revised to generic shape) | APPLIED | `879815d` |
> | P7 — FR-10 new (revised to generic shape) | APPLIED | `879815d` |
> | P8 — FR-11 new (revised to generic shape) | APPLIED | `879815d` |
> | P9 — NFR-11 amendment | APPLIED | `879815d` |
> | P10 — NFR-15 new | APPLIED | `879815d` |
> | P11 — US-13 new (revised: dropped provenance carve-out, per-slot archive) | APPLIED | `3ce9b88` |
> | P12 — US-10 AC-3 worked-example refresh | APPLIED | `0adcb2f` |
>
> This document is retained per the project's no-rm policy so the decision trail is recoverable. The "Application order once approved" section at the bottom is now obsolete; refer to the commits above.

---

# Original proposal text (preserved as a historical record)

This document originally collected every proposed edit to a **governance artefact** required by the v0.5 plan. Per the deputy-project-manager rule, governance artefacts (`docs/project_description.md`, `docs/project_requirements.md`, `docs/stories/US-XX-*.md`) cannot be modified without explicit user approval.

Each section below shows: the target file, the proposed change as drafted at review time, and a short rationale. Notes about how the proposal was actually applied (or revised before application) are recorded inline at the head of each section where they differ from the draft.

The plan itself (`docs/v0.5_implementation_plan.md`) and the protocol additions (`docs/agentic_tool_integration_protocol.md`) were committed earlier — they are not governance artefacts.

---

## P1 — `docs/project_description.md`: glossary updates

**Target lines**: the glossary entry for `customization_type` (currently mid-document, listing `agent` and `skill`).

**Proposed change**: extend the glossary entry to enumerate the v0.5 values.

Existing (paraphrased): "v0.4.1 ships two: `agent` (a single managed file per customization_artifact) and `skill` (a managed folder containing a `SKILL.md` …). Future versions may add `prompt_template`, `mcp_server_config`, etc."

Replacement:

> "v0.5 registers five: `agent` (a single managed file per customization_artifact), `skill` (a managed folder containing a `SKILL.md` written by the agentic_tool's renderer, plus optional auxiliary files), `rules` (a single Markdown file per rule with optional YAML frontmatter), `slash_command` (a single Markdown or TOML file per command with optional frontmatter), and `mcp_server` (one MCP server definition per customization_artifact, projected to one slot inside a shared keyed-map file). An agentic_tool declares which customization_types it can read and write via `supported_customization_types`. Each customization_type has an associated `file_layout` (single file, folder, or shared keyed-map slot) describing how it is stored on disk."

**Rationale**: codifies the v0.5 customization_type set in the project's vocabulary so user stories, requirements, and code can refer to it without re-defining.

## P2 — `docs/project_description.md`: release-history entry

**Target lines**: the bullet list under "Release history" (currently ends with v0.4.1).

**Proposed change**: add a v0.5 bullet:

> "v0.5: three new agentic_tools (Cursor, Gemini CLI, GitHub Copilot) and three new customization_types (`rules`, `slash_command`, `mcp_server`). Antigravity remains the canonical owner of `~/.gemini/antigravity/skills/`; Gemini CLI does not declare `skill` in its `supported_customization_types`. A new top-level config key `mcp_server_secret_policy` (`refuse` / `redact` / `permissive`, default `refuse`) governs how literal secrets in MCP-server `env`, `headers`, and `auth.*` fields are handled."

**Rationale**: per-version release notes are a project-description convention.

## P3 — `docs/project_description.md`: scope update on Cursor partial coverage

**Target lines**: the "Out of scope (initially)" bullet list.

**Proposed change**: add a new bullet making Cursor's SQLite-only surfaces explicitly non-goals:

> "Cursor's in-app settings pane state (`state.vscdb` SQLite), including the User Rules pane, in-app Custom Modes, account-stored Memories, Notepads, and per-account model selection. agents_sync syncs only Cursor's on-disk file surfaces (`.cursor/rules/*.mdc`, `.cursor/commands/`, `.cursor/mcp.json`, the user-scope mirrors). This limitation is intentional and structural: SQLite state is not addressable as files."

**Rationale**: pre-empts user surprise. The Cursor adapter ships with documented partial coverage; the project description records why.

## P4 — `docs/project_requirements.md`: FR-05 amendment

**Target line**: FR-05.

**Proposed change**:

> Existing: "FR-05 (v0.4.1 agent matrix): The daemon shall sync user-level `agent` artifacts across Claude Code, Codex, and opencode when those tools are available."
>
> Replacement: "FR-05 (v0.5 agent matrix): The daemon shall sync user-level `agent` artifacts across Claude Code, Codex, opencode, Gemini CLI, and GitHub Copilot when those tools are available."

Cursor does not declare `agent` (no agent files exist). Antigravity does not declare `agent` (per v0.4.1).

**Rationale**: keeps the per-version concrete-tool matrix accurate for v0.5.

## P5 — `docs/project_requirements.md`: FR-06 amendment

**Target line**: FR-06.

**Proposed change**:

> Existing: "FR-06 (v0.4.1 skill matrix): The daemon shall sync user-level `skill` artifacts across Claude Code, Codex, Google Antigravity, and opencode when those tools are available."
>
> Replacement: "FR-06 (v0.5 skill matrix): The daemon shall sync user-level `skill` artifacts across Claude Code, Codex, Google Antigravity, opencode, and GitHub Copilot when those tools are available."

Cursor and Gemini CLI do not declare `skill` (Cursor has no `SKILL.md` surface; Gemini CLI defers to Antigravity per D3).

**Rationale**: same as P4.

## P6 — `docs/project_requirements.md`: new FR-09 (v0.5 rules matrix)

**Proposed addition** (after FR-08):

> "FR-09 (v0.5 rules matrix): The daemon shall sync user-level `rules` artifacts across Claude Code, Codex, opencode, Gemini CLI, Cursor, and GitHub Copilot when those tools are available."

**Rationale**: mirrors FR-05 / FR-06 for the new customization_type.

## P7 — `docs/project_requirements.md`: new FR-10 (v0.5 slash_command matrix)

**Proposed addition** (after FR-09):

> "FR-10 (v0.5 slash_command matrix): The daemon shall sync user-level `slash_command` artifacts across Claude Code, Codex, opencode, Gemini CLI, Cursor, and GitHub Copilot when those tools are available."

**Rationale**: same as P6.

## P8 — `docs/project_requirements.md`: new FR-11 (v0.5 mcp_server matrix)

**Proposed addition** (after FR-10):

> "FR-11 (v0.5 mcp_server matrix): The daemon shall sync user-level `mcp_server` artifacts across Claude Code, Codex, opencode, Gemini CLI, Cursor, and GitHub Copilot when those tools are available."

**Rationale**: same as P6.

## P9 — `docs/project_requirements.md`: NFR-11 amendment

**Target line**: NFR-11.

**Proposed change**:

> Existing: "NFR-11 (Extensibility): Adding support for a new agentic_tool shall require only a new agentic_tool integration module (per `docs/agentic_tool_integration_protocol.md`) and a matching config entry. The sync engine, conflict resolution, adoption, reconciliation, and removal-propagation code shall be untouched."
>
> Replacement: "NFR-11 (Extensibility): Adding support for a new agentic_tool or a new customization_type shall require only a new agentic_tool integration module — or a new customization_type declaration in `agents_sync.agentic_tool_spec` plus its `file_layout` class, per `docs/agentic_tool_integration_protocol.md` — and a matching config entry. The sync engine, conflict resolution, adoption, reconciliation, and removal-propagation code shall be untouched."

**Rationale**: v0.5 itself is an instance of NFR-11; the wording should make that explicit. The current wording reads as if only new tools are in scope.

## P10 — `docs/project_requirements.md`: new NFR-15 (secret handling)

**Proposed addition** (after NFR-14):

> "NFR-15 (Secret handling): The daemon shall honour the configured `mcp_server_secret_policy` for every `mcp_server` artifact. Accepted values are `refuse` (default), `redact`, and `permissive`. Under `refuse`, the daemon shall fail closed for any `mcp_server` artifact whose canonical document contains a literal value matching the secret-detection heuristics on any of `env`, `headers`, or `auth.*`. Under `redact`, literals shall be replaced with `${env:AGENTS_SYNC_REDACTED_<n>}` placeholders and the redaction recorded in the artifact's canonical document. Under `permissive`, literals shall propagate verbatim and the daemon shall log one structured warning per affected artifact per poll."

**Rationale**: codifies D2 at the requirements level so US-13 and the future Cursor / Gemini / Copilot adapter tests have a single source of truth.

## P11 — new `docs/stories/US-13-customization-type-expansion.md`

**Proposed file**: new user story. Draft below.

```markdown
# US-13: Customization-type expansion (rules, slash_command, mcp_server)

## Persona

Alice

## User Story

As a power user maintaining a library of rules, slash commands, and MCP-server configurations across multiple agentic_tools, I want each of those artefacts to be a first-class managed customization_artifact — discovered, adopted, propagated, reconciled, and archived using the same mechanisms that already work for agents and skills — so that I can edit a rule in any tool and see it appear in every other tool that supports the same customization_type within one polling interval.

## Priority

Should Have

## Terminology

Vocabulary used in this story is defined in the project glossary at `docs/project_description.md`. The `rules`, `slash_command`, and `mcp_server` customization_types and their file_layouts are specified in `docs/agentic_tool_integration_protocol.md` §v0.5 customization_types.

## Acceptance Criteria

- [ ] AC-1 [Normal]: Given an agentic_tool registers `"rules"` in its `supported_customization_types`, When the user creates a Markdown rule file under the tool's configured rules root, Then within two polling intervals every other available agentic_tool that registers `"rules"` exposes an equivalent rule file under its own configured rules root, with the same `customization_artifact_id` injected and recoverable via that tool's `extract_customization_artifact_id`.
- [ ] AC-2 [Normal]: Given an agentic_tool registers `"slash_command"`, When the user creates a command file under the tool's configured commands root, Then within two polling intervals every other available agentic_tool that registers `"slash_command"` exposes an equivalent command file. Body interpolation grammars (`$ARGUMENTS`, `!`-shell, `@`-file, `{{args}}`, `!{cmd}`, `@{path}`) are preserved byte-for-byte by the renderer.
- [ ] AC-3 [Normal]: Given an agentic_tool registers `"mcp_server"`, When the user adds a new server slot under the tool's `SharedKeyedMapLayout.shared_path`, Then within two polling intervals every other available agentic_tool that registers `"mcp_server"` exposes an equivalent slot under its own shared file, with sibling slots in those files preserved byte-for-byte.
- [ ] AC-4 [Normal]: Given a `rules` artifact whose canonical document sets `private: true` (as declared by the source adapter's `parse` function), When the daemon runs, Then no canonical entry is created, no archive write occurs, and the artifact is not propagated to any other agentic_tool. The `private: true` artifact remains on disk untouched on its origin tool.
- [ ] AC-5 [Failure]: Given `mcp_server_secret_policy = "refuse"` (the default), When an `mcp_server` artifact carries a literal value matching the secret-detection heuristics on `env`, `headers`, or `auth.*`, Then the daemon emits a structured error per US-03 AC-10 naming the artifact, the offending field path, and the configured policy. The artifact is NOT adopted. The on-disk file is left untouched.
- [ ] AC-6 [Normal]: Given `mcp_server_secret_policy = "redact"`, When an `mcp_server` artifact carries a literal value matching the secret-detection heuristics, Then the canonical document stores `${env:AGENTS_SYNC_REDACTED_<n>}` in the offending field, the redaction is recorded in `canonical.secret_redactions[]`, and the artifact propagates normally. Each downstream agentic_tool's render emits the placeholder verbatim; the user is expected to set the env var on each host.
- [ ] AC-7 [Normal]: Given `mcp_server_secret_policy = "permissive"`, When an `mcp_server` artifact carries a literal value matching the secret-detection heuristics, Then the artifact propagates unchanged and the daemon logs one structured warning per artifact per poll naming the field path. Loop suppression is unaffected.
- [ ] AC-8 [Normal]: Given an `mcp_server` artifact's slot inside the shared keyed-map file is modified by the daemon, When the prior slot bytes differ from the new slot bytes, Then the prior bytes of that slot only (serialised independently from the surrounding shared file) are archived under `archive/<customization_artifact_id>/<agentic_tool_name>/<slot-key>.<file-extension>.<ISO-timestamp>` per US-05 AC-1's per-artifact attribution rule. Sibling slots in the same shared file whose bytes did not change produce no archive entries on this poll.
- [ ] AC-9 [Failure]: Given a `slash_command` artifact whose `name` collides with an agentic_tool's reserved built-in command names (e.g. opencode's `build` / `plan` / `general` / `explore` / `scout`), When the daemon would create or rename onto that name on that tool, Then the daemon emits a structured warning per US-03 AC-10 and skips that propagation step for that tool only. Other tools receive the artifact normally.

## Notes

- Conflict resolution for `rules`, `slash_command`, and `mcp_server` artifacts uses US-06's existing `most-recent-modification-time wins` rule unchanged. No customization_type-specific conflict semantics are introduced in v0.5.
- The `private` field on the canonical is load-bearing (AC-4 above). The `provenance: "user" | "agent"` field is informational only in v0.5 — it is recorded on the canonical for diagnostics and future reference but has no associated policy. Sources of `provenance: "agent"`: Goose memory extension, Claude Code memory tool output, Gemini CLI `/memory add`-appended blocks. Sources of `private: true`: Windsurf hash-keyed memories, Junie user-scope memory, `.goosehints.local`. Adapters set both at parse time based on the source path.
- Archive granularity for `mcp_server` artifacts follows US-05 AC-1 without exception. The slot's prior bytes are serialised independently (one JSON or TOML fragment per archive entry); the bytes of the surrounding shared file outside the slot's `map_key_path` entry are never archived. This keeps archive browsing symmetric across customization_types.
- Cursor's adapter ships with `rules`, `slash_command`, and `mcp_server` support — no `agent`, no `skill` — because Cursor has no file-based equivalents for the latter two. See `docs/project_description.md` "Out of scope" for the SQLite-only surfaces.
- Gemini CLI's adapter ships with `agent`, `rules`, `slash_command`, and `mcp_server`. It does NOT declare `skill`; Antigravity remains the canonical owner of `~/.gemini/antigravity/skills/` per the path-ownership model (see plan §Resolved design decisions D3).
- GitHub Copilot's adapter ships with `agent`, `skill`, `rules`, `slash_command`, and `mcp_server`. Coverage spans the CLI half (`~/.copilot/`) and the VS Code user-profile half (the per-OS `prompts/` directory plus user `mcp.json`).

Related requirements: FR-09 (rules matrix — load-bearing for AC-1), FR-10 (slash_command matrix — AC-2, AC-9), FR-11 (mcp_server matrix — AC-3, AC-8), NFR-11 (extensibility), NFR-15 (secret handling — AC-5, AC-6, AC-7).
```

**Rationale**: US-13 captures the user-visible v0.5 capabilities. AC-1–AC-3 cover the per-type round-trip; AC-4–AC-5 codify the `private` and `provenance` semantics; AC-6–AC-8 the three secret-redaction policy paths; AC-9 the SharedKeyedMapLayout archive rule; AC-10 the reserved-name constraint.

## P12 — `docs/stories/US-10-extensible-agentic_tool-registry.md`: AC-3 example update

**Target line**: AC-3, which today says: "Example: if `claude` and `codex` support `{agent, skill}` and `antigravity` supports `{skill}` only, then agent customization_artifacts flow over `{claude, codex}`, while skill customization_artifacts flow over `{claude, codex, antigravity}`."

**Proposed change**:

> Replacement: "Example: if `claude` and `codex` support `{agent, skill, rules, slash_command, mcp_server}`, `antigravity` supports `{skill}` only, `gemini_cli` supports `{agent, rules, slash_command, mcp_server}`, and `cursor` supports `{rules, slash_command, mcp_server}`, then `agent` customization_artifacts flow over `{claude, codex, gemini_cli}`; `skill` over `{claude, codex, antigravity}`; `rules` over `{claude, codex, gemini_cli, cursor}`; and so on for each customization_type."

**Rationale**: AC-3 illustrates the participation rule with a worked example. The current example references only `agent` and `skill`; updating it to span the v0.5 customization_types makes the rule's mechanics concrete in v0.5 terms.

The "Notes" section of US-10 mentions "the user-facing README and the project description enumerate the concrete agentic_tools shipped in each release; this story does not". The v0.5 customization_type set lives in the project description (P1); US-10 stays tool-agnostic.

---

## Application order once approved

1. Apply P1, P2, P3 to `docs/project_description.md` (one commit, message `docs: extend glossary, release history, and scope for v0.5`).
2. Apply P4–P10 to `docs/project_requirements.md` (one commit, message `docs: add FR-09/10/11 and NFR-15, amend FR-05/06 and NFR-11 for v0.5`).
3. Apply P11 — create `docs/stories/US-13-customization-type-expansion.md` (one commit, message `docs(stories): add US-13 customization-type expansion`).
4. Apply P12 to `docs/stories/US-10-extensible-agentic_tool-registry.md` (one commit, message `docs(stories): broaden US-10 AC-3 example to v0.5 customization_types`).
5. Delete this file (`docs/v0.5_governance_proposals.md`) as a follow-up commit, message `docs: remove v0.5 governance proposals after application`.

If you'd rather apply a subset, the items are independent and can land separately. If you reject any item, that decision is recorded by leaving the proposal in this file with an `IMPLEMENTATION_PLAN.md`-style "REJECTED" tag and a one-line reason.
